import { createChatBotMessage } from 'react-chatbot-kit';

const config = {
  botName:"Artive CHATBOT",
  initialMessages: [createChatBotMessage(`Welcome This is artive Chatbot`)],
};

export default config;
